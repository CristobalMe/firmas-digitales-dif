# firmas-digitales-dif

To run the project:
```bash
git clone firmas-digitales-dif
cd firmas-digitales-dif
docker-compose up -d --build
```
Then, open your browser and go to `http://localhost:3000`.
You should see the application running.

